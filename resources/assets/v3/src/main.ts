// Main entry point for Firefly III v3
// Test integration with Inertia middleware and TypeScript

import type { SharedData } from '@/types';

// Test the TypeScript configuration and Inertia integration
console.log('Firefly III v3 - TypeScript and Inertia middleware ready!');

// This will be replaced with actual Vue app initialization in future phases
// For now, we just export a test to verify the configuration works
export default {
  message: 'Firefly III v3 configuration test complete'
} as const;
